# CLAUDE.md

This file exists so Claude Code (and any other agent that looks for a
`CLAUDE.md`) lands on the right instructions.

**The entry point for working on this repo is [`AGENTS.md`](AGENTS.md).** Read it
in full before editing anything — it explains the one rule that matters most here
(keep the state machine in sync across all seven files) and the invariants you
must never break.

> If you were sent here to *run the loop* on some other project, you want that
> project's own `AGENTS.md`, not this one — and specifically its `## Loop
> Commands` section. Nothing in this repo is the runtime.

Quick map:

- **Working on loop-vibe-coding itself** → [`AGENTS.md`](AGENTS.md)
- **"Where do I change X?"** → [`docs/architecture.md`](docs/architecture.md)
- **What the project is / how to adopt it** → [`README.md`](README.md)
